# Cheat Console Extended 上下文

## 领域边界

本项目面向《Blasphemous 1》的游戏内控制台，目标是在保留原版控制台语义的前提下扩展其能力与可维护性。

## 术语

### 原版控制台（vanilla console）

游戏本身提供的调试控制台及其命令集合，是玩家提交命令、执行游戏状态操作和查看输出的领域边界。

_Avoid_: 将前置 `CheatConsole` mod 与游戏本身的控制台混称。

### 原版命令（vanilla command）

游戏运行时直接装配到原版控制台命令列表中的顶级命令入口；游戏提供的共享命令虽然同样来自原版内容，也不属于这一类。

_Avoid_: 仅因命令随游戏发布，就把它归入原版命令；应区分直接注册的命令入口与共享命令脚本。

### 顶级命令（top-level command）

输入行中用于选择一个命令入口的第一个词；一个入口可以拥有别名，也可以继续分派到子命令。

_Avoid_: 将顶级命令、子命令和整行原始输入视为同一概念。

### 子命令（subcommand）

顶级命令选中后，用于表达具体动作的第二层词及其参数。

_Avoid_: 将子命令当作可以脱离所属顶级命令独立解析的命令。

### 扩展命令（mod command）

由 mod 提供、通过前置 `CheatConsole` 接入原版控制台的命令；对玩家而言仍属于同一个控制台命令空间。

_Avoid_: 将扩展命令理解为独立的 UI 按钮或独立输入系统。

### 共享命令（shared command）

由游戏内容提供、以 ID 标识的可复用命令脚本，由多行既有控制台输入组成；它是独立于原版命令和扩展命令的第三种命令来源。执行共享命令时，脚本中的输入按顺序重新交给控制台处理，因此共享命令本身不是原版直接命令入口，也不是扩展命令。

_Avoid_: 将共享命令与单次历史记录、原版命令入口、普通扩展命令或命令别名混为一谈；“游戏提供”不等于“直接注册到原版命令列表”。

### 严格命令匹配（strict command matching）

顶级命令和子命令只有在名称完整匹配（大小写不敏感）时才会被解析；名称前缀不再自动作为命令执行。

_Avoid_: 将前缀缩写或近似建议当作已确认的命令执行。

### 命令补全（command completion）

玩家在控制台输入过程中按 Tab 对当前顶级命令或子命令词进行大小写不敏感的前缀补全；补全只替换文本，不执行命令。

_Avoid_: 将命令补全理解为严格命令匹配、命令建议或自动执行。

### 补全候选菜单（completion candidate menu）

由控制台扩展添加的临时 UI，用于显示多个命令补全候选；玩家可用上下方向键移动选中项、用 Tab 采用选中项或用 Escape 关闭菜单。

_Avoid_: 将补全候选菜单理解为原版输出、Help 列表或命令执行面板。

### 命令建议距离（command suggestion distance）

用于限制字符串近似建议候选的 Damerau-Levenshtein 距离上限；它只影响错误输入后的提示，不改变严格命令匹配规则。值为 0 时不提供建议，负值会被规范化为 0。

_Avoid_: 将命令建议距离理解为允许命令执行的模糊匹配范围。

### 命令副作用（command side effect）

命令执行后对玩家、世界、UI、进度或存档产生的状态变化；控制台输出本身不等同于副作用。

_Avoid_: 只以“是否打印文本”判断命令是否安全或可重复。

### 前置 CheatConsole

上游 `Blasphemous.CheatConsole` mod，负责让原版控制台可用，并为其他 mod 提供扩展命令接入能力。

_Avoid_: 将前置 mod 的职责与本仓库的扩展职责合并。

### 控制台扩展（CheatConsole Extended）

本仓库正在构建的 mod，依赖原版控制台和前置 `CheatConsole`，用于改善命令、交互和执行路径。

_Avoid_: 默认假设本项目会替换整套原版控制台。

## 控制台显示语义

### 控制台颜色配置

控制台用于区分输入角色和命令输出的四个独立、可持久化的用户设置；默认提供顶级命令、子命令、参数和输出的颜色。

_Avoid_: 将颜色配置理解为命令解析、命令执行规则或错误分类。

### 命令回显

控制台对用户提交的输入行进行的可见记录；它保留用户输入原文，并与命令执行输出分开。

_Avoid_: 将命令回显与共享命令内部脚本的隐式执行混淆。

### 颜色角色

控制台显示中用于区分顶级命令、子命令、参数和输出的四个语义位置；颜色角色只影响可视呈现，不参与解析或执行。

_Avoid_: 将颜色角色理解为命令有效性、错误类型或命令副作用的标记。

### 系统字体（OS-installed font）

当前用户已经安装、并可由 Unity 按字体族名解析的字体。控制台字体功能只消费这类字体；mod 不携带、安装、注册、覆盖或删除字体文件。

_Avoid_: 将 TTF 文件名、字体族名和 Unity `Font` 对象视为同一个概念。

### 控制台字体（console font）

应用于控制台输入文本、占位文本以及控制台输出文本的 `UnityEngine.Font`。它只影响控制台 UI，不扩展到其他游戏 UI。

_Avoid_: 将控制台字体设置理解为全局替换游戏字体。
